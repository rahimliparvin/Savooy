@using Savoy.Areas.Admin.ViewModels.BlogVM;
@model List<BlogIndexVM>


@{
    ViewData["Title"] = "Index";
   
}





<div style="padding-left:200px" class="container">

    <a asp-action="Create" style="border:1px solid green;margin-bottom:20px;;margin-top:20px;margin-left:850px;text-decoration-color:white;" class="btn btn-success">Create</a>

    <div class="tab-content" id="orders-table-tab-content">
        <div class="tab-pane fade show active" id="orders-all" role="tabpanel" aria-labelledby="orders-all-tab">
            <div class="app-card app-card-orders-table shadow-sm mb-5">
                <div class="app-card-body">
                    <div class="table-responsive">
                        <table class="table app-table-hover mb-0 text-left">
                            <thead>
                                <tr>
      
                                    <th class="cell">Image</th>
                                    <th class="cell">Title</th>
                                    <th class="cell">Author</th>
                                    <th style="width: 340px;" class="cell">Operations</th>

                                </tr>
                            </thead>
                            <tbody>

                                @foreach (var blog in Model)
                                {
                                    
                                    <tr>
                                       
                                        <td class="cell">
                                            <img style="width:70px;height:70px;" src="~/assets/image/@blog.Image">
                                        </td>
                                        <td class="cell">
                                            @blog.Title
                                        </td>
                                        <td class="cell">
                                            @blog.Author
                                        </td>
                                        <td class="cell">
                                            <a asp-action="Detail" asp-route-id="@blog.Id" style="border:1px solid blue" class="btn btn-outline-info">Info</a>
                                            <a asp-action="Edit" asp-route-id="@blog.Id" style="border:1px solid green" class="btn btn-outline-primary">Edit</a>
                                            <form style="display:inline-block" asp-action="Delete" asp-route-id="@blog.Id" method="post">
                                                <button type="submit" style="border:1px solid red" class="btn btn-outline-danger">Delete</button>
                                            </form>
                                        </td>

                                    </tr>
                                }


                            </tbody>
                        </table>
                    </div><!--//table-responsive-->

                </div><!--//app-card-body-->
            </div><!--//app-card-->


        </div><!--//tab-pane-->




    </div><!--//tab-content-->
@*
    <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center">
            <li class="page-item @(Model.HasPrevious? "":"disabled")">

                <a class="page-link" asp-action="Index" asp-route-page="@(Model.CurrentPage - 1)" tabindex="-1">Previous</a>
            </li>

            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(Model.CurrentPage == i ? "disabled":"")"><a class="page-link" asp-action="Index" asp-route-page="@i">@i</a></li>

            }

            <li class="page-item @(Model.HasNext? "":"disabled")">
                <a class="page-link" asp-action="Index" asp-route-page="@(Model.CurrentPage + 1)">Next</a>
            </li>
        </ul>
    </nav>*@


</div>



@*


<div class="container my-5">
    <a class="btn btn-success" style="float: right;" asp-action="Create">Create</a>

    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>
                        #
                    </th>
                    <th>
                        Image
                    </th>
                    <th>
                        Title
                    </th>
                    <th>
                        Author
                    </th>
                    <th>
                        Operations
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var blog in Model.Datas)
                {
                    count++;
                    <tr>

                        <td>
                            @count
                        </td>
                        <td class="py-1">
                            <img src="~/assets/images/website-images/@blog.MainImage" style="width: 70px; height: 70px" alt="image" />
                        </td>

                        <td>
                            @blog.Title
                        </td>

                        <td>
                            @blog.AuthorName
                        </td>



                        <td>

                            <a class="btn btn-outline-info" asp-action="Detail" asp-route-id="@blog.Id">Info</a>
                            <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@blog.Id">Edit</a>

                            <form style="display: inline-block;" asp-action="Delete" asp-route-id="@blog.Id" method="post">
                                <button type="submit" class="btn btn-outline-danger">Delete</button>
                            </form>


                        </td>

                    </tr>

                }


            </tbody>
        </table>
    </div>

    <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center">
            <li class="page-item @(Model.HasPrevious? "":"disabled")">

                <a class="page-link" asp-action="Index" asp-route-page="@(Model.CurrentPage - 1)" tabindex="-1">Previous</a>
            </li>

            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(Model.CurrentPage == i ? "disabled":"")"><a class="page-link" asp-action="Index" asp-route-page="@i">@i</a></li>

            }

            <li class="page-item @(Model.HasNext? "":"disabled")">
                <a class="page-link" asp-action="Index" asp-route-page="@(Model.CurrentPage + 1)">Next</a>
            </li>
        </ul>
    </nav>



</div>

*@
